<% sidebar_items << capture do %>
	<h2><%= @user.full_name %></h2>
	<ul>
		<li><%= link_to('Edit Your Account', edit_user_registration_path(current_user)) %></li>
	</ul>
<% end %>

<%= render partial: "user_items", user_artifacts: @user.artifacts %>